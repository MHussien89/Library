const BASE_URL: string = 'https://reactnd-books-api.udacity.com';

export const APIS = {
    GET_ALL_BOOKS: `${BASE_URL}/books`,
    SEARCH_FOR_BOOKS: `${BASE_URL}/search`,
    UPDATE_BOOK: `${BASE_URL}/books/{bookId}`
};

